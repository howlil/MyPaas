services:
  ghost:
    image: ghost:6-alpine
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "2368:2368"
    environment:
      url: http://localhost:2368
      database__client: mysql
      database__connection__host: db
      database__connection__user: ghost
      database__connection__password: compatibility-password
      database__connection__database: ghost
    volumes:
      - ghost-content:/var/lib/ghost/content
  db:
    image: mysql:8.4
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ghost
      MYSQL_USER: ghost
      MYSQL_PASSWORD: compatibility-password
      MYSQL_ROOT_PASSWORD: compatibility-root-password
    volumes:
      - ghost-mysql:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -pcompatibility-root-password --silent"]
      interval: 10s
      timeout: 5s
      retries: 10

volumes:
  ghost-content:
  ghost-mysql:
